# Overview

Include a brief overview of the project, include:

- How do you deploy and run the project?
- What are its core dependencies?
- Who is it for and why?

## Requirements

This project requires the Tesseract OCR binary to be installed on the system in addition to the Python dependencies. On macOS, install it with:

```bash
brew install tesseract
```

The Python wrapper `pytesseract` is used to access the OCR engine from the project code.
